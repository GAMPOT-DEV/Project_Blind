using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempleTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var _renderer = GetCoomponent<Renderer>();        
    }

}
